
/*

$Revision$
$Author$
$Date$
$Workfile$
$Archive$

Структура данных clients_carriers в 1С

Автор: Елена Шкляр
Дата создания: 26/09/17
Author: Shklyar Elena
Creation date: 26/09/17

*/

using Progress.Lang.*.
using ibs.th.bge.1crn.subjects.subjects from propath.
using ibs.th.bge.1crn.subjects.clients_carriers from propath.

class ibs.th.bge.1crn.subjects.clients_carriers inherits ibs.th.bge.1crn.subjects.asubject:

  define property code_       as integer    no-undo get. 
    private set.
  
  define override property InitTag as character no-undo init "nb-carrier"
    get. 
    
  method override protected void SetPropProtected (inTag as char, inValue as char):
    case inTag:
      when "code" then 
        do:
          assign 
            code_ = integer(inValue) .
        end.
     end case.
  end.

 
  method override protected void CheckData ():

    def var v-str as char no-undo.
    
    assign
      v-str = v-str + ",code" 
      when code_ = ?
      .
    v-str = trim (v-str, ",").
    if v-str > ''
      then 
      undo, throw new Progress.Lang.AppError( substitute ("Теги &1 не могут иметь неопределное значение", v-str)).

  end.
  
  method override protected void ClearSub():
    assign 
      code_         = ?
      .
  end.

end class.