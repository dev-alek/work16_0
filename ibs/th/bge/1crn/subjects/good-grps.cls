/*

$Revision$
$Author$
$Date$
$Workfile$
$Archive$

Структура данных good-grps из 1С

Автор: Александр Морозов
Дата создания: 26/09/17
Author: Morozov Alexandr
Creation date: 26/09/17

*/

using Progress.Lang.*.
block-level on error undo, throw.


class ibs.th.bge.1crn.subjects.good-grps inherits ibs.th.bge.1crn.subjects.asubject: 
  define override public property vss-revision    as character no-undo init "$Revision:$":U get .
  define override public property vss-author      as character no-undo init "$Author:$":U get.
  define override public property vss-date        as character no-undo init "$Date:$":U get.
  define override public property vss-workfile    as character no-undo init "$Workfile:$":U get.
  define override public property vss-archive     as character no-undo init "$Archive:$":U get.
   
  define property code_ as character no-undo
    get. 
    private set.
  define property del-l as integer no-undo 
    get. 
    private set.
  define property del-f as integer no-undo 
    get. 
    private set.
  define property upper-code as character no-undo 
    get. 
    private set.
  define property name_ as character no-undo 
    get. 
    private set.
  define property rn-code as character no-undo 
    get. 
    private set.
  define property r-mat as integer no-undo 
    get. 
    private set.  
  define property Prohibition-sale as integer no-undo 
    get. 
    private set.  

  define override property InitTag as character no-undo init "good-grp"
    get. 
    
  method OVERRIDE PROTECTED void SetPropProtected (inTag as char, inValue as char):
    case inTag:
      when "code" then 
        do:
          assign 
            code_ = inValue .
        end.
      when "del-l" then 
        do:
          assign 
            del-l = integer (inValue).
        end.
      when "del-f" then 
        do:
          assign 
            del-f = integer (inValue).
        end.
      when "upper-code" then 
        do:
          assign 
            upper-code = inValue.
        end.
      when "name" then 
        do:
          assign 
            name_ = inValue.
        end.
      when "rn-code" then 
        do:
          assign 
            rn-code = inValue.
        end.
      when "r-mat" then 
        do:
          assign 
            r-mat = integer(inValue).
        end.
      when "Prohibition-sale" then 
        do:
          assign 
            Prohibition-sale = integer(inValue).
        end.
    end case.
  end .

  method override protected void CheckData ():

    def var v-str as char no-undo.
    
    assign
      v-str = "code" 
      when code_ = ?
      /*      v-str = v-str + ",upper-code" when upper-code = ?*/
      v-str = v-str + ",name" 
      when name_ = ?
      /* v-str = v-str + ",rn-code" 
      when rn-code = ?
      v-str = v-str + ",r-mat" 
      when r-mat = ? */
      .
    v-str = trim (v-str, ",").
    if v-str > ''
      then 
      undo, throw new Progress.Lang.AppError( substitute ("Теги &1 не могут иметь неопределное значение", v-str)).

  end.
  
  method override protected void ClearSub():
    assign 
      code_   = ?
      del-l   = ?
      del-f   = ?
      name_   = ?
      /*      upper-code = ?*/
      rn-code = ?
      r-mat   = ?
      Prohibition-sale = ?
      .
  end.
  

  
end class.